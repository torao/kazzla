<% if model.errors.any? %>
	<div id="error_explanation">
		<h2><%= model.errors.count %>件のエラーがあります。</h2>

		<ul>
			<% model.errors.full_messages.each do |msg| %>
				<li><%= msg %></li>
			<% end %>
		</ul>
	</div>
<% end %>